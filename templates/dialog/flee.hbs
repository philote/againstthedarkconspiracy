<form>
    <p>
        {{{localize "ATDC.dialog.flee.description"}}}
    </p>
    <form class="flexcol">
        <div class="form-group">
            <input type="checkbox" id="baseDie" name="baseDie" checked>
            <label for="baseDie">
                {{{localize "ATDC.dialog.flee.baseDie"}}}
            </label>
        </div>
        <div class="form-group">
            <input type="checkbox" id="expertiseDie" name="expertiseDie" {{#if expertiseUsed}}disabled{{/if}}>
            <label for="expertiseDie">
                {{{localize "ATDC.dialog.flee.expertiseDie"}}}
            </label>
        </div>
        <div class="form-group">
            <input type="checkbox" id="stressDie" name="stressDie">
            <label for="stressDie">
                {{{localize "ATDC.dialog.flee.stressDieMessage"}}} <b style="color: {{riskDieColor}}">{{{localize "ATDC.dialog.flee.stressDieWords"}}}</b>
            </label>
        </div>
    </form>
    </br>
</form>